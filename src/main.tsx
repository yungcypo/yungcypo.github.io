import React from 'react'
import ReactDOM from 'react-dom/client'
import Head from './Head'

ReactDOM.createRoot(document.getElementById('head') as HTMLElement).render(
  <React.StrictMode>
    <Head />
  </React.StrictMode>,
);
